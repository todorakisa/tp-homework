<%= form_for([@encrypt_message.rsa, @encrypt_message]) do |f| %>
  <% if @encrypt_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@encrypt_message.errors.count, "error") %> prohibited this encrypt_message from being saved:</h2>

      <ul>
      <% @encrypt_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
